echo "[INFO] more can only page forwards."
echo -e "$ more filename\n"
echo "[INFO] less can page forwards and backwards, supports regex, and can display a contents file before reading it entirely."
echo "$ less filename"
