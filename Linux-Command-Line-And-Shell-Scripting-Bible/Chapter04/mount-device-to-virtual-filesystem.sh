echo "[INFO] The Linux filesystem combines all media disk into a single virtual directory. To add new media to this virtual directory, you need to mount it first."
echo "# mount -t filesystem-type /path/to/device /mount/path/on/virtual-directory"
