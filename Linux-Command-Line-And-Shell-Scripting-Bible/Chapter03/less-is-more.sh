echo "# more can only page forwards."
echo "$ more filename"
echo "# less can page forwards and backwards, supports regex, and can display a contents file before reading it entirely."
echo "$ less filename"
