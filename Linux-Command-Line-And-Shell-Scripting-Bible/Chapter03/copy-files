echo "# cp source destination"
echo "$ cp /absolute/path/to/source/file relative/path/to/target/file"
echo "# To copy all files from the source recursively use -R which will also create any non-existing folders at the target."
echo "$ cp -R files/to/recursively/copy target/path/to/possibly/create"
